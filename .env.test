# Variables de entorno para el entorno de pruebas (Vitest)
# Vite carga este archivo automáticamente cuando MODE=test
# Las variables VITE_* se reemplazan en tiempo de transformación → axiosClient las lee correctamente
VITE_API_URL=http://localhost:8080/api
