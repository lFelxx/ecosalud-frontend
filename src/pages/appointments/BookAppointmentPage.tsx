import { Box, Typography } from '@mui/material';

export default function BookAppointmentPage() {
  return (
    <Box>
      <Typography variant="h4">Book Appointment Page</Typography>
    </Box>
  );
}
