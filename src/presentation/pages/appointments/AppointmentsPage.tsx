import { Box, Typography } from '@mui/material';

export default function AppointmentsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Citas — próximamente</Typography>
    </Box>
  );
}
